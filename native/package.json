{
  "name": "native",
  "version": "0.2.2",
  "description": "Orbita Native Core",
  "main": "index.js",
  "scripts": {
    "build": "napi build --platform --release",
    "build-static": "set RUSTFLAGS=-C target-feature=+crt-static && napi build --platform --release",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "saizzi",
  "license": "ISC",
  "type": "commonjs",
  "dependencies": {
    "@napi-rs/cli": "^3.6.2"
  },
  "devDependencies": {
    "@napi-rs/wasm-runtime": "^0.2.4"
  }
}