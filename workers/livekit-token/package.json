{
  "name": "orbita-livekit-token-worker",
  "version": "1.0.0",
  "main": "dist/worker.js",
  "scripts": {
    "build": "esbuild worker.ts --bundle --format=esm --platform=node --target=node18 --outfile=dist/worker.js",
    "deploy": "npm run build && npx wrangler deploy",
    "start": "npx wrangler dev"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260719.1",
    "esbuild": "^0.23.0",
    "typescript": "^5.5.3",
    "wrangler": "^4.112.0"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.112.3",
    "livekit-server-sdk": "^2.5.0"
  }
}
